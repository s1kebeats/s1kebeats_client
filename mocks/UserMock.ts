import { User } from '@/api/models';

const UserMock: User = {
  email: 'user@example.com',
  username: 'username',
  id: 0,
  image: '/path/to/image',
  displayedName: 'displayedName',
};
export default UserMock;
